VITE_API_URL=http://localhost:8000

# Google OAuth Client ID (Web application) for "Sign in with Google".
# Public value — safe to commit/ship. Must match the Client ID the backend
# is configured to trust. Get it from your partner / Google Cloud Console.
VITE_GOOGLE_CLIENT_ID=
